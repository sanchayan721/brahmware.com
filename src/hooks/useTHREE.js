/* import { useRef, useEffect } from "react";
import * as THREE from 'three';

const useTHREE = (renderGame, dependencies) => {
    const ref = useRef();

    useEffect(() => {
        renderGame(THREE.)
    })
} */